import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.yagizuzunalioglu.ionicupload',
  appName: 'ionicupload',
  webDir: 'www'
};

export default config;
